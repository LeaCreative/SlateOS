package slate.app

import androidx.appcompat.app.AppCompatActivity

/**
 * Base for companion screens so AppCompatDelegate day/night actually swaps
 * Material3 colours (plain ComponentActivity only updated system bars).
 */
open class SlateActivity : AppCompatActivity()
