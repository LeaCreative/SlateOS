kotlin.code.style=official
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true

# Prefer the Microsoft JDK 17 already on this machine (Android Studio sync).
# Comment out if you point Studio's Gradle JDK elsewhere. The drive-letter colon
# and the separators both need escaping: this is a java.util.Properties file, so
# ':' is a key/value separator and '\' starts an escape.
org.gradle.java.home=C\:\\Program Files\\Microsoft\\jdk-17.0.19.10-hotspot
org.gradle.configuration-cache=true
