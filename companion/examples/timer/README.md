# Timer (sample JS sub-app)

Downloaded JavaScript only. Packaged under `companion/examples/timer/` and
copied into the runtime classpath as `slate/subapps/timer/`.

See [docs/script-runtime.md](../../docs/script-runtime.md) for the no-phone
emulator workflow.
