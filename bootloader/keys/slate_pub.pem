# Placeholder — replace with a real pubkey before production:
#   imgtool keygen -k slate_priv.pem -t ecdsa-p256
#   imgtool getpub -k slate_priv.pem -o slate_pub.pem
#   python scripts/gen_pubkey_c.py
#
# Until then src/mcuboot_pubkey.cpp embeds a zero key (MCUBoot will reject
# signed images). Never commit slate_priv.pem.
